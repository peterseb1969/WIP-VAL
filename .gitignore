node_modules/
dist/
.env
# stray tarballs stay out, but the bundled WIP lib tarballs must be tracked —
# CI (GitHub runner) builds images from the repo alone, and npm ci installs
# @wip/* from libs/ (see package.json file: deps)
*.tgz
!libs/*.tgz
.DS_Store
.claude/
yac-discussions

# scratch verification scripts (run via tsx; not part of the build)
roundtrip-vendor.ts
preflight-test.ts
# binary test fixtures
test-data/
# stray local artifacts
.claude-context-pct
Users/
